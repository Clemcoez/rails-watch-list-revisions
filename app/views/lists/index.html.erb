<div class="container">
  <div class="row">
    <div class="col">
      <h1 class="mt-4">Lists</h1>
      <ul class="mt-5">
        <% @lists.each do |list| %>
            <li> <%= list.name %> </li>
          <% end %>
      </ul>
    </div>
  </div>
</div>
